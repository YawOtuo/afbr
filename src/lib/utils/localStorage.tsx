// // customHooks.js
// import { useLocalStorage as useLocalStorageClient } from "@uidotdev/usehooks";

// export const useLocalStorage = (key) => {
//   return typeof window !== 'undefined' ? useLocalStorageClient(key) : [null, () => {}];
// };
